services:

  minio-bootstrap:

    container_name: retailpulse-minio-bootstrap

    build:
      context: ./docker/bootstrap/minio
      dockerfile: Dockerfile

    env_file:
      - .env

    depends_on:
      - minio

    restart: "no"

    networks:
      - retailpulse-network